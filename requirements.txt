# CodeTruth Agent V2 - Python Dependencies
#
# Install with:
#     pip install -r requirements.txt
#
# Python version required: 3.11 or newer

# -----------------------------------------------------------
# Core: Semantic engine (deterministic, no LLM)
# -----------------------------------------------------------

# Sentence-transformer embedding model.
# First run downloads ~90MB model file to local cache.
sentence-transformers>=2.5.0,<3.0

# Numerical operations (used by embedding pipeline)
numpy>=1.26.0

# -----------------------------------------------------------
# Core: Standard library extensions
# -----------------------------------------------------------

# JSON5 / robust JSON parsing for governance memory files
# (Optional but recommended for malformed JSON tolerance)
# json5>=0.9.0   # uncomment if json5 is actually used in codebase

# -----------------------------------------------------------
# Notes:
# - The Python standard library (ast, hashlib, pathlib, json,
#   re, os, sys) provides everything else V2 needs.
# - V2 has no LLM API dependencies. No OpenAI / Anthropic /
#   Google keys required.
# - The V1 components in core/ require zero external packages.
# -----------------------------------------------------------
